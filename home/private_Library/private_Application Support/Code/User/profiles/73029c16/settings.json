// Profile: Terraform
// Profile path: ~/Library/Application Support/Code/User/profiles/73029c16/settings.json
// NOTE: useDefaultFlags.settings is true — VS Code reads the root Code/User/settings.json for this profile; this file is not currently active.
{
  // Inherit all settings from the Default profile. Only extensions are different
  //
  // General settings
  // Git settings
  // Editor settings
  // UI settings
  // Search settings
  // Terminal settings
  // Language-specific settings
  // Other settings
}
