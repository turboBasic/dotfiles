# secrets
*.private.env.json
*.key
accounts.json
/home/tmp

# other project files
*megalinter_file_names_cspell.txt
http-client.env.json
#work.gitconfig

# macOS files
.DS_Store
Desktop.ini
._*
Thumbs.db

# Python files
.venv
__pycache__/
**/__pycache__/
*.py[cod]
*$py.class

# Temporary files
tmp/
*.log

# Node.js
node_modules/

# MegaLinter
megalinter-reports/

# Terraform
.terraform
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*tfplan*

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version
# control as they are data points which are potentially sensitive and subject
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore files used for debugging Terraform code
debug.tf

# Zsh files
*.zwc
